export default class TodoComment {
    /**
     * @param {number} id
     * @param {string} content
     * */
    constructor(id, content) {
        this.id = id;
        this.content = content;
    }
}
